/**
 * Premium Glass Effect Theme Utilities
 * 
 * Reusable glass morphism classes for consistent premium UI across the application.
 * All cards should use these utilities to maintain visual consistency.
 */

/**
 * Primary glass card - for main KPI cards, summary cards, and important content
 */
export const glassCardPrimary = 
  'bg-white/10 dark:bg-white/5 backdrop-blur-md border border-white/20 rounded-2xl shadow-lg'

/**
 * Secondary glass card - for category cards, transaction rows, and secondary content
 */
export const glassCardSecondary = 
  'bg-white/5 dark:bg-white/5 backdrop-blur-sm border border-white/10 rounded-xl shadow-md'

/**
 * Glass banner - for welcome banners and prominent sections
 */
export const glassBanner = 
  'backdrop-blur-md border border-white/20 rounded-2xl shadow-lg'

/**
 * Glass section - for background sections with gradients
 */
export const glassSection = 
  'backdrop-blur-md border border-white/20 rounded-xl shadow-lg'

/**
 * Glass filter/input - for filters, dropdowns, and input containers
 */
export const glassFilter = 
  'bg-white/5 dark:bg-white/5 backdrop-blur-sm border border-white/10 rounded-lg'
