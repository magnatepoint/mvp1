name: Deploy Backend to Production

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      - '.github/workflows/deploy-backend.yml'
  workflow_dispatch:  # Allow manual trigger

jobs:
  deploy:
    name: Deploy to Ubuntu Server
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Verify Secrets
        run: |
          if [ -z "${{ secrets.SSH_PRIVATE_KEY }}" ]; then
            echo "❌ ERROR: SSH_PRIVATE_KEY secret is not set!"
            echo "Please add it at: https://github.com/${{ github.repository }}/settings/secrets/actions"
            exit 1
          fi
          if [ -z "${{ secrets.SERVER_HOST }}" ]; then
            echo "❌ ERROR: SERVER_HOST secret is not set!"
            exit 1
          fi
          if [ -z "${{ secrets.SERVER_USER }}" ]; then
            echo "❌ ERROR: SERVER_USER secret is not set!"
            exit 1
          fi
          echo "✅ All secrets are configured"
      
      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
      
      - name: Add server to known hosts
        run: |
          ssh-keyscan -H ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts || {
            echo "⚠️  Warning: ssh-keyscan failed, but continuing..."
            echo "This is usually okay if the server is already in known_hosts"
          }
      
      - name: Deploy to server
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} << 'ENDSSH'
            set -e
            cd /opt/mvp-backend/backend
            
            # Use the deployment script
            if [ -f "deploy/scripts/deploy-remote.sh" ]; then
              bash deploy/scripts/deploy-remote.sh
            else
              # Fallback to inline deployment
              echo "📥 Pulling latest code..."
              git fetch origin
              git reset --hard origin/main
              git clean -fd
              
              echo "🔨 Rebuilding Docker images..."
              docker-compose -f docker-compose.yml -f docker-compose.prod.yml build --no-cache
              
              echo "🗄️  Running database migrations..."
              ./deploy/scripts/run-migrations.sh || echo "⚠️  Migrations failed (continuing anyway)"
              
              echo "🛑 Stopping existing services..."
              docker-compose -f docker-compose.yml -f docker-compose.prod.yml down || true
              
              echo "🚀 Starting services..."
              docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d
              
              echo "⏳ Waiting for services to be ready..."
              sleep 15
              
              echo "🏥 Running health check..."
              ./deploy/scripts/health-check.sh || echo "⚠️  Health check had issues"
              
              echo "✅ Deployment complete!"
            fi
          ENDSSH
      
      - name: Deployment Status
        if: success()
        run: |
          echo "✅ Deployment successful!"
      
      - name: Deployment Failed
        if: failure()
        run: |
          echo "❌ Deployment failed! Check the logs above."
